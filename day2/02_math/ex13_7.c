#include <stdio.h>
#include <math.h>

int main() {
    // pow(밑, 지수), sqrt(값), log10(값) 예제
    printf("pow(5, 3) = %.1lf \n", pow(5.0, 3.0));
    printf("sqrt(25) = %.1lf \n", sqrt(25.0));
    printf("log10(1000) = %.1lf \n", log10(1000.0));
    
    return 0;
}
